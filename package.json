{
  "name": "twilio-virtual-phone-backend",
  "version": "1.0.0",
  "description": "Twilio Virtual Phone Backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon -r dotenv/config ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install && npm run build && mv build/ ../public && cd ../ && rm -rf client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "path": "^0.12.7",
    "pg": "^8.7.1",
    "redis": "^4.0.3",
    "socket.io": "^4.4.1",
    "twilio": "^3.73.0"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "dotenv": "^14.2.0",
    "nodemon": "^2.0.15"
  }
}
